[workspace]
resolver = "2"
members = ["."]

[package]
name = "rust-app"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.dev]
opt-level = 0
debug = true
panic = "unwind"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
debug = false
panic = "abort"
strip = "symbols"
