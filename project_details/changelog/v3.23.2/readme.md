# v3.23.2 Release Notes

## Bugfixes
* **UX:** Rebound the `[Enter]` key to automatically open the Directory Browser instead of skipping to the next step when viewing the `Deployment Target` step.
* **UX:** Upon successfully selecting a directory via `[Space]` in the Directory Browser, the system now safely auto-advances the Wizard sequence straight to `Step 2: Artifacts`.
