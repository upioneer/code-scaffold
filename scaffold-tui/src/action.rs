#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Action {
    Tick,
    Render,
    Resize(u16, u16),
    Suspend,
    Resume,
    Quit,
    Refresh,
    Error(String),
    Help,
    Up,
    Down,
    Left,
    Right,
    Enter,
    Tab,
    ShiftTab,
    Execute,
    Char(char),
    Backspace,
    AgentInstructions,
    OpenIde,
}
